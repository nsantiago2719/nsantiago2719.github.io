document.addEventListener("DOMContentLoaded", function(event) { 
  var swiper = new Swiper(".details-swiper", {
    effect: "cards",
    grabCursor: true,
    keyboard: {
      enabled: true,
    }
  });
});     